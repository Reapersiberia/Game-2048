# 🧪 Как проверить работу кнопок gm и деплоя контракта

## Способ 1: Локальная проверка в браузере (быстрый способ)

1. **Откройте `index.html` в браузере:**
   - Просто дважды кликните на файл `index.html`
   - Или откройте через браузер: `Ctrl+O` → выберите `index.html`

2. **Проверьте визуально:**
   - Должны быть видны две кнопки: **"gm"** (зеленая) и **"Деплой контракта"** (оранжевая)
   - Кнопки расположены между счетчиком и игровым полем

3. **Проверьте функциональность:**
   - Нажмите кнопку **"gm"** → должна открыться новая вкладка с `https://warpcast.com/~/compose?text=gm`
   - Нажмите кнопку **"Деплой контракта"** → должна открыться новая вкладка с `https://build.base.org/`

## Способ 2: Проверка через консоль браузера

1. Откройте `index.html` в браузере
2. Нажмите `F12` (или `Ctrl+Shift+I`) для открытия DevTools
3. Перейдите на вкладку **Console**
4. Проверьте наличие элементов:
   ```javascript
   // Проверка наличия кнопок
   document.getElementById('gm-btn')
   document.getElementById('deploy-contract-btn')
   
   // Проверка наличия SDK (если открыто в Farcaster/Base app)
   window.miniapp
   ```

5. Проверьте обработчики событий:
   ```javascript
   // Симуляция клика на кнопку gm
   document.getElementById('gm-btn').click()
   
   // Симуляция клика на кнопку деплоя
   document.getElementById('deploy-contract-btn').click()
   ```

## Способ 3: Проверка через локальный сервер (рекомендуется)

1. **Запустите локальный сервер:**
   
   **Вариант A: Python (если установлен)**
   ```bash
   # Python 3
   python -m http.server 8000
   
   # Или Python 2
   python -m SimpleHTTPServer 8000
   ```
   
   **Вариант B: Node.js (если установлен)**
   ```bash
   npx http-server -p 8000
   ```
   
   **Вариант C: VS Code Live Server**
   - Установите расширение "Live Server"
   - Правый клик на `index.html` → "Open with Live Server"

2. **Откройте в браузере:**
   - Перейдите на `http://localhost:8000` (или порт, который указали)
   - Проверьте работу кнопок

## Способ 4: Проверка в Farcaster/Base app (полная проверка)

1. **Убедитесь, что приложение задеплоено на Vercel:**
   - Проверьте, что изменения запушены в GitHub
   - Дождитесь автоматического деплоя на Vercel

2. **Откройте приложение в Base app:**
   - Создайте пост с URL вашего приложения
   - Или откройте напрямую через Base app

3. **Проверьте работу кнопок:**
   - В контексте Farcaster SDK кнопки должны использовать `miniapp.sdk.actions.openUrl()`
   - Это откроет URL внутри приложения, а не в новой вкладке

## Что проверить:

✅ **Визуальная проверка:**
- [ ] Кнопки отображаются на странице
- [ ] Кнопка "gm" зеленая
- [ ] Кнопка "Деплой контракта" оранжевая
- [ ] Кнопки расположены между счетчиком и игровым полем

✅ **Функциональная проверка:**
- [ ] Клик на "gm" открывает URL с Warpcast compose
- [ ] Клик на "Деплой контракта" открывает Base Build
- [ ] В консоли нет ошибок при клике

✅ **Проверка в консоли:**
- [ ] Элементы кнопок найдены в DOM
- [ ] Обработчики событий прикреплены
- [ ] SDK доступен (если открыто в Farcaster app)

## Возможные проблемы:

❌ **Кнопки не отображаются:**
- Проверьте, что файл `style.css` загружен
- Проверьте консоль на ошибки загрузки CSS

❌ **Кнопки не работают:**
- Откройте консоль (F12) и проверьте ошибки
- Убедитесь, что `script.js` загружен полностью
- Проверьте, что обработчики событий прикреплены

❌ **URL не открывается:**
- Проверьте интернет-соединение
- Убедитесь, что URL правильные в `script.js`
- Проверьте, не блокирует ли браузер всплывающие окна

## Быстрая проверка через PowerShell:

```powershell
# Открыть index.html в браузере по умолчанию
Start-Process "index.html"
```

---

**После проверки:** Если все работает локально, закоммитьте и запушьте изменения, чтобы они появились на Vercel!
